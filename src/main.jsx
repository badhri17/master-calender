import React from "react";
import ReactDOM from "react-dom/client";
import WeekCalendar from "./week2-calendar";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WeekCalendar />
  </React.StrictMode>
);

